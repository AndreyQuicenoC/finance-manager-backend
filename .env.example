# ============================================
# Finance Manager Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# In production, set these variables in your hosting platform (Render, Heroku, etc.)
# ============================================

# ============================================
# REQUIRED - Application Configuration
# ============================================

# Node Environment (development | production)
NODE_ENV=development

# Server Port (default: 3000)
PORT=3000

# ============================================
# REQUIRED - Security
# ============================================

# JWT Secret Key (generate a strong random string)
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# REQUIRED - Email Service (SendGrid)
# ============================================

# SendGrid API Key
# Get from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your-sendgrid-api-key

# SendGrid Verified Sender Email
# Must be verified in SendGrid dashboard
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# ============================================
# REQUIRED - Frontend URLs
# ============================================

# Frontend URL for Development
FRONTEND_URL_DEV=http://localhost:3000

# Frontend URL for Production
FRONTEND_URL_PROD=https://your-frontend-domain.com

# ============================================
# OPTIONAL - CORS Configuration
# ============================================

# CORS Origin (if different from FRONTEND_URL)
# If not set, will use FRONTEND_URL_PROD or FRONTEND_URL_DEV based on NODE_ENV
# CORS_ORIGIN=https://your-frontend-domain.com

# ============================================
# OPTIONAL - Database (if using external Prisma)
# ============================================

# Database URL (if not using Prisma's default)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ============================================
# Notes:
# ============================================
# 1. Never commit .env file to version control
# 2. In production, set variables in your hosting platform:
#    - Render: Environment tab in dashboard
#    - Heroku: heroku config:set KEY=value
#    - Railway: Variables tab in dashboard
# 3. JWT_SECRET should be a long, random string
# 4. SENDGRID_FROM_EMAIL must be verified in SendGrid
# 5. FRONTEND_URL_PROD should match your frontend domain exactly
